version: v2
name: jonathan-crm

build:
  method: docker
  context: .
  dockerfile: ./Dockerfile

services:
  - name: web
    type: web
    port: 80
    cpuCores: 0.1
    ramMegabytes: 128
    autoscaling:
      enabled: false
    instances: 1
