version: v2
name: jonathan-crm

build:
  method: pack
  context: .
  builder: heroku/builder:22
  buildpacks:
    - heroku/nodejs

services:
  - name: web
    type: web
    run: npm start
    port: 3000
    cpuCores: 0.1
    ramMegabytes: 256
    autoscaling:
      enabled: false
    instances: 1
